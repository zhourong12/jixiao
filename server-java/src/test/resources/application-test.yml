spring:
  datasource:
    url: jdbc:h2:mem:jixiao2;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:

jixiao2:
  session:
    secret: test-secret-for-unit-tests
