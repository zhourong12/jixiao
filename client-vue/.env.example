# 前端 API 基址：留空 = 使用相对路径（推荐本地 + Vite 开发）
# - npm run dev（Vite）：请求发到当前页同源（如 http://localhost:5174），由 vite.config.ts 把 /auth、/api 代理到 VITE_API_TARGET（默认 http://127.0.0.1:8081）。
# - 排障：若 /auth/feishu/subjects 返回 404，先直接访问后端是否通，例如浏览器打开 http://127.0.0.1:8081/auth/feishu/subjects 应得到 JSON（items 数组）；若此处也 404，说明 8081 上不是本 Java 服务或未启动成功。
# - 直连后端（不走 Vite 代理）：可设为 VITE_API_BASE_URL=http://127.0.0.1:8081（需后端已开且 CORS 允许当前前端源）
# - 本地开发：Vite 已代理 /auth、/api、/actuator；可在浏览器打开 http://localhost:5174/actuator/mappings 核对路由（与后端 application.yml 暴露项一致）
VITE_API_BASE_URL=

# 仅 Vite dev / preview 的 proxy target；与上一行留空配合使用
VITE_API_TARGET=http://127.0.0.1:8081
