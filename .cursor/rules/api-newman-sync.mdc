---
description: 后端 HTTP API 变更时同步 API 清单与 Newman 集合
globs:
  - server/modules/**/*.controller.ts
  - shared/api.interface.ts
---

# API 清单与 Newman 同步

凡对 **对外 HTTP 接口** 做 **新增、修改路径/方法、删除、请求体或鉴权方式变更**（以 `server/modules/**/*.controller.ts` 及相关的 `shared/api.interface.ts` 为准），在同一提交或同一任务内必须完成：

1. 更新 **`scripts/newman/api-inventory.md`** 中的对应行（方法、路径、鉴权、简要说明）。
2. 更新 **`scripts/newman/build-collection.mjs`**（增删改请求项、示例 Body/Query、文件夹划分），并执行 **`npm run newman:build`** 重新生成 **`scripts/newman/jixiao2-api.postman_collection.json`**。
3. 若 `local.postman_environment.json` 需要新变量（如路径参数、示例 ID），一并补上并在 `README.md` 中必要时一行说明。

无需同步的情况：仅内部 service 重构且 Controller 对外契约未变；纯前端路由或非 JSON 的页面渲染未动 API。
